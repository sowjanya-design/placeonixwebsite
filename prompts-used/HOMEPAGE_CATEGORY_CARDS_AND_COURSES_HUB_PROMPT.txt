=====================================================================
PLACEONIX — COLLAPSE HOMEPAGE PROGRAM GRID INTO CATEGORY CARDS
+ NEW "ALL COURSES" HUB PAGE + NEW "ALL SAP COURSES" HUB PAGE
+ SITE-WIDE "EXPLORE PROGRAMS" LINKS POINT TO THE HUB
Paste into your IDE agent. Check-gate at the end of each part.
=====================================================================

CONTEXT (confirmed in code):
- public/index.html, section id="programs" (~line 2409-2440), has a
  `.prog-grid` of 21 individual `.prog-card` links — 10 SAP course
  cards (sap-btp, sap-cpi, sap-capm, sap-rap, sap-ui5-fiori, sap-abap,
  sap-fico, sap-mm, sap-sd, sap-basis), 6 Technology cards, and 5 Combo
  Package cards (including the highlighted "SAP Full Stack Program"
  card using `.sap-highlight`/`.sap-inner`).
- There is currently no single page listing ALL courses, and no single
  page listing ALL SAP courses — only the individual course pages
  themselves under /courses/sap/*.html, /courses/technology/*.html,
  /courses/combo/*.html.
- "Explore Programs" and similar CTAs currently link to `#programs`
  (the homepage section itself), e.g. line 2248
  `<a href="#programs" class="clean-btn-secondary">Explore Programs</a>`.
  There are likely other buttons/links across the site with similar
  wording or `#programs` hrefs — grep for them (see Part 3).

REQUESTED CHANGE: Instead of showing all 21 individual course cards on
the homepage, show a small set of CATEGORY cards (one "SAP Courses"
card, one "Technology Courses" card, one "Combo Programs" card — your
call on whether Combo stays a separate category card or folds into
one of the others, but keep it simple: 3 category cards total is the
target). Clicking a category card goes to a new hub page listing every
course in that category. Additionally, every "Explore Programs" /
"Programs" link anywhere on the site should point to a new master
"All Courses" hub page (not to `#programs` on the homepage anymore).

=====================================================================
PART 1 — BUILD THE HUB PAGES
=====================================================================
1. Create `public/courses/index.html` — the master "All Courses" hub.
   - Use the site's existing page shell (same <head> boilerplate, nav,
     footer, design tokens as any course page — copy the structure
     from an existing page like public/courses/sap/sap-btp.html for
     the shared header/footer/nav, then replace the body).
   - Body: reuse the exact `.prog-grid` / `.prog-card` markup and CSS
     classes already defined in site.css (this component already
     exists and is styled — do not reinvent it), just move ALL 21
     existing cards (the same hrefs, tags, copy — do not rewrite the
     course descriptions) from index.html's #programs section into
     this new page, organized under three subheadings: "SAP Courses",
     "Technology Courses", "Combo Programs" (simple `<h2>` or
     `.sec-hdr` dividers between groups, reusing existing heading
     styles — no new components).
   - Give it a proper <title>, meta description, canonical tag, and
     breadcrumb (Home > Courses), consistent with how every other
     course page is set up.
2. Create `public/courses/sap/index.html` — the "All SAP Courses" hub.
   - Same page shell approach as above.
   - Body: just the 10 SAP `.prog-card` entries (BTP, CPI, CAPM, RAP,
     UI5/Fiori, ABAP, FICO, MM, SD, BASIS) — same hrefs/copy/tags as
     currently on the homepage, unchanged.
   - Title/meta/canonical/breadcrumb (Home > SAP Courses).
3. Add both new hub pages to `public/sitemap.xml`.
4. Add both new hub URLs into the SAP Courses / Technology Courses /
   Combo Programs nav dropdowns (as a "View All SAP Courses →" /
   "View All Courses →" link inside each relevant dropdown, alongside
   the existing individual links — don't remove the individual links
   from the nav, this is additive).

CHECK GATE — PART 1:
  [ ] /courses/index.html loads, lists all 21 courses correctly
      grouped, every card link works (no 404s).
  [ ] /courses/sap/index.html loads, lists all 10 SAP courses, every
      card link works.
  [ ] Both pages visually match the existing course-page design
      language (same header/footer/nav/typography) — not a different
      style.
  [ ] Both added to sitemap.xml.

=====================================================================
PART 2 — COLLAPSE THE HOMEPAGE GRID INTO 3 CATEGORY CARDS
=====================================================================
1. In public/index.html, section id="programs" (~line 2409-2440),
   REPLACE the 21 individual `.prog-card` links with exactly 3 category
   cards using the same `.prog-card` class/styling (so it still looks
   native to the site, not a new component):
   - "SAP Courses" card → links to /courses/sap/index.html. Copy should
     summarize the category, e.g. "10 SAP programs — BTP, CPI, FICO,
     ABAP, MM, SD, BASIS and more. Cloud, functional and core modules
     for every SAP career path." Keep a relevant tag/badge (e.g. "Very
     High Demand") and meta line (e.g. "⏱ 6 weeks – 3 months · 🏢 10
     Programs").
   - "Technology Courses" card → links to a new
     /courses/technology/index.html hub (build this the same way as
     Part 1's hubs — same pattern, just for the 6 technology courses:
     Data Science, DevOps/Cloud, Full Stack, Java Backend,
     Cybersecurity, UI/UX). Add this hub page now, it wasn't listed
     explicitly above but follows the identical pattern.
   - "Combo Programs" card → links to a new
     /courses/combo/index.html hub (same pattern, for the 5 combo
     packages). Add this hub page now too, same pattern as Part 1.
   - Keep the existing section heading/eyebrow/description
     ("Flagship Premium Programs" / "SAP, technology programs and
     combo packages...") — still accurate for 3 category cards.
2. Since the homepage grid now only has 3 cards, adjust `.prog-grid`'s
   column count for this specific instance if needed (e.g. a
   `grid-template-columns: repeat(3, 1fr)` variant) so 3 cards don't
   look sparse/stretched — check both `.prog-grid` in site.css and any
   homepage-specific override in index.html's inline style block (the
   two-stylesheet drift issue has bitten this project before — check
   BOTH places).
3. Confirm mobile (375px): 3 stacked category cards should look
   intentional, not like a broken partial grid.

CHECK GATE — PART 2:
  [ ] Homepage #programs section shows exactly 3 category cards (SAP /
      Technology / Combo), each linking to its correct new hub page.
  [ ] /courses/technology/index.html and /courses/combo/index.html
      exist and work (built as part of this section).
  [ ] Homepage grid looks intentional at 1440px and 375px — no
      leftover 4-column grid styling stretching 3 cards oddly.
  [ ] All 21 original course pages still exist and are unchanged —
      this task only changes what's LINKED from the homepage, not the
      course pages themselves.

=====================================================================
PART 3 — "EXPLORE PROGRAMS" / "PROGRAMS" LINKS SITE-WIDE → COURSES HUB
=====================================================================
1. Grep the entire public/ directory for anchor text like "Explore
   Programs", "Programs", "View Programs", "See Programs", "All
   Programs" and any href="#programs" — this pattern likely appears in
   the homepage hero CTA (line ~2248), the nav, and possibly repeated
   in course-page templates (check public/courses/**/*.html too, not
   just index.html).
2. Change every one of these to link to `/courses/index.html` (the new
   master hub from Part 1) INSTEAD of `#programs`. Exception: if a
   button's clear intent is "jump to the programs section on THIS
   page" in a context where that still makes sense (unlikely after
   this change, since the homepage section is now just 3 cards) — use
   your judgment, but the default per this request should be: any
   "Explore Programs" / generic "Programs" CTA anywhere on the site
   goes to the courses hub page, not an anchor scroll.
3. Do NOT change links that already point to a SPECIFIC course or
   category (e.g. "SAP BTP" nav links, individual course card links,
   the SAP-category card from Part 2) — this only affects the GENERIC
   "explore all programs" style CTAs.

CHECK GATE — PART 3:
  [ ] Every generic "Explore Programs"/"Programs" CTA site-wide
      (homepage hero, any repeated header/footer CTA, course-page CTAs
      if present) now points to /courses/index.html.
  [ ] No specific/individual course link was accidentally changed.
  [ ] Quick click-through test: from 3 different pages, click an
      "Explore Programs"-style button and confirm it lands on the
      courses hub every time.

=====================================================================
FINAL CHECK GATE (whole task)
=====================================================================
  [ ] Homepage shows 3 category cards, not 21 individual ones.
  [ ] 4 new hub pages exist and work: /courses/index.html,
      /courses/sap/index.html, /courses/technology/index.html,
      /courses/combo/index.html.
  [ ] All 4 added to sitemap.xml with proper canonical/meta tags.
  [ ] Nav dropdowns include a "View All" link per category alongside
      existing individual links.
  [ ] Every generic "Explore Programs" CTA site-wide points to
      /courses/index.html.
  [ ] No existing course page, URL, or redirect from prior work broke
      (quick spot-check 3 individual course pages still load fine).
  [ ] Mobile (375px) check on homepage category cards and both new hub
      pages.

Start with Part 1 now.
