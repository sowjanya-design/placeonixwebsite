=====================================================================
PLACEONIX — FIX THE FOOTER LOGO (stop using a white sticker on dark bg)
Paste into your IDE agent.
=====================================================================

PROBLEM
public/logo.jpeg is a JPEG with a solid white background baked in (no
transparency possible in JPEG) and a dark navy wordmark. On the dark
(--ink/#0b1020-ish) site-footer, this currently gets wrapped in a white
rounded card (see .foot-logo in public/assets/css/site.css and/or the
inline footer logo styling in public/index.html's <style> block) to hide
the JPEG's white background. That card reads as an odd floating sticker
rather than a blended logo — enlarging it will only make the sticker
bigger, not fix the mismatch. The real problem is the asset, not the size.

TWO NEW ASSETS ALREADY GENERATED (use these, don't regenerate):
- public/assets/img/logo-transparent.png — transparent background, ORIGINAL
  colors (purple mark + dark navy wordmark). Use this anywhere the logo sits
  on a LIGHT background (site nav, light sections) instead of logo.jpeg.
- public/assets/img/logo-white.png — transparent background, wordmark
  recolored to white and the purple mark lightened slightly for contrast.
  Use this ONLY on dark backgrounds (the footer, any dark-mode section).

WHAT TO DO
1. Footer (public/index.html and the shared footer markup used by
   public/courses/*.html): replace the <img src="/logo.jpeg" class="foot-logo">
   with <img src="/assets/img/logo-white.png" class="foot-logo" alt="Placeonix" width="180" height="66">.
   Remove the white-card CSS treatment entirely — no background, no padding,
   no box-shadow, no border-radius on .foot-logo. Let the logo sit directly
   on the dark footer background, same as the preview: clean, no box.
   Set a sensible height (e.g. 44-52px, width: auto) so it feels proportionate
   next to the footer column headings — do NOT just blow up the old asset.
2. Site nav (.nav-logo) and anywhere else logo.jpeg is used on a LIGHT
   background: switch to /assets/img/logo-transparent.png instead of
   logo.jpeg — same visual, but no visible white rectangle if the nav
   background is ever not pure white (e.g. the sticky nav's blurred/
   translucent background per site.css .site-nav rgba(255,255,255,.96)).
3. Leave public/logo.jpeg in place and still referenced for favicon /
   apple-touch-icon / Open Graph / Twitter / JSON-LD "logo" and "image"
   fields (public/index.html head tags) — those contexts need a flat image
   with a background and don't need transparency; changing them isn't
   necessary and could break social-preview rendering.
4. Check both style systems — public/assets/css/site.css (course pages) AND
   the inline <style> block in public/index.html (homepage) — the footer
   logo styling may be duplicated in both; fix it in both places consistently.
5. After the change, visually confirm on both the homepage and at least one
   course page: no white box/shadow around the footer logo, wordmark is
   clearly readable in white against the dark footer, mark still reads as
   the brand's purple, and sizing looks proportionate at both desktop and
   mobile widths (this pairs with the mobile-responsiveness pass already
   requested — check the footer logo specifically at 375px too).

Do not change anything else about the footer layout, only the logo image
and its wrapper styling.
