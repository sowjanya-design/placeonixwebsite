=====================================================================
PLACEONIX — NAV MEGA-MENU RESTRUCTURE + COURSE-PAGE POLISH
Paste into your IDE agent. Check-gate at the end of each part before moving on.
=====================================================================

CONTEXT (confirmed in code — verify current state before editing, it may have
moved since this was written):
- The shared nav (public/assets/css/site.css + repeated in every page's
  <nav class="site-nav">) currently has: a "SAP Courses" dropdown (.has-dropdown
  / .dropdown.wide) listing all 10 SAP course pages — this part works well —
  but Data Science and GenAI are lone top-level links, and Cloud/DevOps, Full
  Stack, Java Backend, Cybersecurity, UI/UX Design, and all 5 combo pages are
  NOT in the nav at all. That's why a student on a course page (e.g.
  java-backend-training.html) only ever sees "SAP Courses" as a real menu —
  everything else is invisible/unreachable from the nav.
- All course pages currently live flat under public/courses/*.html (e.g.
  /courses/sap-btp-training.html, /courses/java-backend-training.html) with
  real canonical URLs, JSON-LD, and sitemap.xml entries already pointing at
  those flat paths.
- Dropdown hover behavior already exists via CSS (:hover / :focus-within on
  .has-dropdown, opacity/visibility/transform transition ~.18s) and already
  collapses to a static in-menu list on mobile (<768px) — reuse this pattern,
  don't rebuild it from scratch.

=====================================================================
PART 1 — REGROUP THE NAV INTO CATEGORY DROPDOWNS (do this first, no URL
changes yet, so the site is never broken mid-way)
=====================================================================
1. Restructure the nav into clearly grouped dropdowns so EVERY course is
   reachable, not just SAP:
   - "SAP Courses" dropdown — keep as-is (all 10 SAP course pages).
   - "Technology Courses" dropdown (new) — Data Science, Generative AI,
     Cloud/DevOps, Full Stack Web Development, Java Backend Development,
     Cybersecurity Fundamentals, UI/UX Design.
   - "Combo Programs" dropdown (new) — SAP Integration Expert, AI + Data
     Science, Cloud Dev Bundle, SAP Functional Analyst, SAP Full Stack
     Program.
   - Keep Blog / Placements / Contact / Apply Now as top-level links, same as
     today.
2. Apply this SAME nav markup to every page — the shared nav block appears
   in public/index.html AND at the top of every public/courses/*.html file.
   Update all of them consistently (search-and-replace the nav block, don't
   hand-edit each one differently).
3. Animation polish: refine the existing dropdown transition in site.css
   (.dropdown / .has-dropdown) so it feels intentional rather than an instant
   pop — e.g. a slightly longer, eased fade + 6-8px slide-down (build on the
   existing opacity/visibility/transform pattern, don't introduce a new
   animation library). Keep it fast (150-220ms) so it doesn't feel laggy.
   Confirm keyboard/touch access still works (:focus-within already handles
   keyboard; on mobile the dropdown already renders as a static open list —
   keep that, don't try to force hover on touch devices).
4. Confirm every one of the (eventual) 22 course/combo pages is reachable
   from this nav — cross-check against public/courses/*.html and flag any
   page that still doesn't exist yet so it can be tracked, but don't block
   this nav update on missing pages — link the ones that exist now, and note
   the rest for later.

CHECK GATE — PART 1:
  [ ] Every existing course page is linked from one of the three dropdowns.
  [ ] Nav markup is identical/consistent across all pages (no drift between
      index.html and the course pages).
  [ ] Dropdown opens/closes smoothly on hover (desktop) and works correctly
      inside the mobile hamburger menu (test at 375px).
  [ ] Keyboard navigation (Tab + focus) still opens the dropdowns.
Report before Part 2.

=====================================================================
PART 2 — URL RESTRUCTURE TO CATEGORY FOLDERS (courses/sap/sap-btp.html style)
=====================================================================
IMPORTANT TRADEOFF TO UNDERSTAND FIRST: the current flat URLs
(/courses/sap-btp-training.html) already have canonical tags, JSON-LD, and
sitemap.xml entries pointing at them, and may already be indexed by Google.
Restructuring URLs without redirects breaks those and causes a real, if
temporary, SEO setback. Do this properly, not casually:

1. Move course pages into category folders, e.g.:
   /courses/sap/sap-btp.html, /courses/sap/sap-cpi.html, ...
   /courses/technology/data-science.html, /courses/technology/java-backend.html, ...
   /courses/combo/sap-integration-expert.html, ...
   (Use clean, consistent slugs — drop the repeated "-training" suffix where
   the folder already implies it, e.g. /courses/sap/sap-btp.html not
   /courses/sap/sap-btp-training.html — your call, but be consistent across
   all pages.)
2. For EVERY moved page, add a 301 redirect from its old flat URL to the new
   nested URL (this is a static Vercel deployment — add redirect rules to
   vercel.json under a "redirects" array; check the current vercel.json first,
   it already has a "headers" array, add "redirects" alongside it, not
   replacing it).
3. Update EVERY internal reference to the old URLs, across the whole site:
   - The nav dropdowns from Part 1.
   - Homepage program cards (public/index.html).
   - Every course page's own canonical tag, Open Graph URL, Twitter URL, and
     JSON-LD "url" field.
   - The "Related Programs" internal-linking blocks (if already built).
   - The SAP BTP vs SAP ABAP comparison page (if already built).
   - public/sitemap.xml — replace every old URL with the new one (don't just
     add new entries and leave stale old ones).
   - public/robots.txt if it references specific paths.
4. Do NOT change public/logo.jpeg, public/assets/*, or anything outside
   public/courses/ — this is a URL/path change for course pages only.

CHECK GATE — PART 2:
  [ ] Every new nested URL loads correctly (no 404s) — list them and confirm.
  [ ] Every old flat URL 301-redirects to its new nested URL — test at least
      5 of them directly.
  [ ] No page anywhere on the site still links to an old flat URL (grep the
      whole public/ directory for "courses/sap-" and similar old patterns
      after the move — there should be zero remaining references outside the
      redirect rules themselves).
  [ ] sitemap.xml contains only the new URLs.
  [ ] Canonical/OG/Twitter/JSON-LD on each page reflect its own new URL.
Report before Part 3.

=====================================================================
PART 3 — REMOVE "IN HYDERABAD" FROM VISIBLE HEADLINES
=====================================================================
Requested: remove "in Hyderabad" from the visible page headings (H1s currently
read like "SAP BTP Training in Hyderabad", "Java Backend Development Training
in Hyderabad").

SEO NOTE (apply this compromise rather than deleting the signal outright): the
earlier SEO work specifically added "Hyderabad" to titles/H1s because it's the
#1 local-search ranking pattern this market uses (see docs/seo/ or the SEO
keyword research if present in the repo). Fully removing the city from
everywhere would undo that. So:
  - Remove "in Hyderabad" from the visible <h1> text on every course page and
    the homepage, per the request.
  - KEEP "Hyderabad" in: the <title> tag, the meta description, the JSON-LD
    (Course/Organization "areaServed" or description fields), and the small
    eyebrow line above the H1 (e.g. change "Technology · High Demand" to
    "Technology · High Demand · Hyderabad" or similar) so the geo-signal isn't
    lost for search engines and AI answers, just decluttered from the big
    headline text itself.
  - Apply this consistently across all course pages and the homepage H1.

CHECK GATE — PART 3:
  [ ] No visible H1 contains "in Hyderabad" anymore (spot check 5 pages).
  [ ] Every page's <title> and meta description still contain "Hyderabad".
  [ ] Every page's eyebrow/kicker line still signals Hyderabad in some small,
      unobtrusive way.
Report before Part 4.

=====================================================================
PART 4 — RECOMMENDED "MAKE THIS PAGE EXCELLENT" POLISH
=====================================================================
Beyond what was explicitly asked, apply these to every course page template
(same template used across all course/combo pages, so one fix = all pages):

1. In-page mini-nav: add a small sticky sub-nav or set of anchor chips near
   the top of the content area linking to Curriculum / Careers & Salary / FAQ
   sections on the same page — these pages are long, and readers currently
   have no way to jump around without scrolling blind.
2. Make the "Course Snapshot" card (duration/mode/level/prerequisite/
   certification) sticky on desktop as the user scrolls through the long page
   content, so the Request Brochure / Book Free Counselling CTA stays visible
   without hunting for it (common, proven pattern for long course pages).
3. Update the breadcrumb to reflect the new category structure from Part 2,
   e.g. Home > SAP Courses > SAP BTP (matching the new nav grouping and URL).
4. Confirm every course page already has (add if missing): the
   "Curriculum at a Glance" visual near the top (already built for the
   original 15 pages via scripts/inject-glance.js — extend it to any newer
   pages like java-backend-training.html that may not have it yet), a
   "Related Programs" block linking 2-3 relevant other courses using the NEW
   nested URLs, and a real FAQ block.
5. Double-check the WhatsApp float button and sticky mobile CTA don't overlap
   the new in-page mini-nav on small screens.
6. Re-run a quick version of the mobile-responsiveness check (375px width) on
   two or three pages after all these changes, since nav markup, URLs, and
   H1s all changed in this pass — confirm nothing broke.

CHECK GATE — PART 4:
  [ ] Mini-nav / anchor links present and working on all course pages.
  [ ] Snapshot card sticky behavior works cleanly on desktop, doesn't break
      on mobile (should NOT be sticky on mobile — screen too small).
  [ ] Breadcrumbs match the new URL structure everywhere.
  [ ] Every course page has: curriculum-at-a-glance, Related Programs block,
      and a real FAQ block.
  [ ] Quick mobile spot-check at 375px on 3 pages shows no new breakage.

FINAL DELIVERABLE: docs/audit/nav-and-urlchange-report.md listing the old →
new URL for every moved page, confirming every redirect works, confirming the
nav is consistent site-wide, and noting any course pages still missing so they
can be tracked separately.

Start with Part 1 now.
